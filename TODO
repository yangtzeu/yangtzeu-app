登录功能
班级列表功能

======================================================================================

查人页面获取院系班级列表的__VIEWSTATE和__EVENTVALIDATION可以复用，因此，在第一次通过网络获取到
院系列表时可以将其缓存到数据库，以后直接从数据库读取，避免从网络获取，节流。