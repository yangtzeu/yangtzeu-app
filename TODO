直接从网上下载下来的数据应当解析后直接渲染界面，待界面绘制完成后将此数据存储到数据库#作为缓存#，而
不是将网上获取的数据先放入数据库再从数据库获取数据渲染界面，因为这样会导致数据库不可避免的读写频繁
而发生#数据库锁定#或是阻塞界面的状况。

获取数据应当先检测数据库#缓存#是否存在#可用#数据，若存在可用数据，则直接从数据库读取缓存数据而避免
从网上下载。

查人页面获取院系班级列表的__VIEWSTATE和__EVENTVALIDATION可以复用，因此，在第一次通过网络获取到
院系列表时可以将其缓存到数据库，以后直接从数据库读取，避免从网络获取，节流。